命名与打包示例：用户导出 50 张标签图时，可设置文件名模板 Label_{recordIndex}_{customerName}.png，导出后自动打包成 2025-11-04-Labels.zip，内含按顺序命名的 PNG。
数据源辅助工具示例：在对话框里增加一块“数据源浏览器”，列表显示记录号、主要字段值；用户点击第 18 条即可跳转预览，若某条缺字段会高亮提示，方便迅速定位并修正数据。

排版导出:
可选增强（可后续再做）：

单元格内外边距与内容缩放策略可配置（现在是保持比例居中铺满单元格边界）。
支持记录区间（按打印对话框的范围）导出。
支持导出 PNG/JPEG 的排版版本（目前“下载”已有单记录导出，未做网格排版）

现在几何图形除了直线，矩形，圆形外，我还想添加几个特殊图形，添加多边形、星形、箭头